phantom: "1"
project: "escape-path"
name: "Escape Path"

setup:
  type: web
  run:
    command: "npm run dev"
    ready_check:
      type: http
      url: "http://localhost:3000"
      timeout: 10

captures:
  - id: home
    name: "Home"
    output: "../../etc/evil.png"

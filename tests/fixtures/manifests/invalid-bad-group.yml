phantom: "1"
project: "bad-group"
name: "Bad Group"

setup:
  type: web
  run:
    command: "npm run dev"
    ready_check:
      type: http
      url: "http://localhost:3000"
      timeout: 10

captures:
  - id: home
    name: "Home"
    output: "docs/home.png"

groups:
  main:
    - home
    - nonexistent-capture
